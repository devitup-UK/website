export default {
    name: 'Box',
    props: {
        outline: Boolean,
        dashed: Boolean,
        dark: Boolean,
        condensed: Boolean,
        icon: Object,
        content: String
    }
}